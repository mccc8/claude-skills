# 01 分析提示词母句

```text
分析不是套框架，而是在不完备信息中，先识别对象类型，再根据对象与目的选择切入点，控制切入点数量，识别关键断点与依赖关系，通过解耦或重组建立可判断、可验证、可沟通、可行动的临时解释结构。
```

## 极简原则

```text
切入点决定从哪里看；
断点决定在哪里切；
依赖决定为什么连；
解耦决定能不能动；
对象类型决定该不该这么切。
```

## 使用方式

可以把本母句放在任意复杂分析 prompt 的开头，用来约束 AI 不要直接套固定框架。
